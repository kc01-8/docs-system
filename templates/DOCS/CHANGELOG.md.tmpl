# {{PROJECT_NAME}} — Change Log

> Record of changes. Newest first. Keep this updated whenever behavior, APIs, files, or known issues
> change — see [README.md](README.md).
>
> **Archival rule:** keep this file under **500 lines**. When it exceeds 500, cut the oldest entries
> (keep the most recent ~6 months) into `DOCS/archive/CHANGELOG-archive-NN.md` (zero-padded,
> incrementing: `-01`, `-02`, …). Leave a pointer line under this header:
> `Older entries: [archive/CHANGELOG-archive-01.md].` Never delete history — move it. Do the cut as
> its own commit so diffs stay readable.

<!-- Older entries: none archived yet. -->

---

## {{TODAY}} — Docs system initialized

- Scaffolded `DOCS/` (ARCHITECTURE, MODULE_MAP, USER_MANUAL, CHANGELOG, ROADMAP) and `CLAUDE.md`
  via the docs-system plugin's `/docs-init`.

<!-- Entry format: `## YYYY-MM-DD — Short title`, then bullets. Lead each bullet with the **bold
     essence** of the change; include renamed/new public symbols by name so MODULE_MAP diffs are
     traceable. Group same-day related changes under one heading. -->
