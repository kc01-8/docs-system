# {{PROJECT_NAME}} — User Manual

> **Audience: humans using {{PROJECT_NAME}}.** This is the reference for the domain and for observable
> behavior — what things mean and how to use them. It is authoritative on *behavior and domain*, not on
> code structure (that's [ARCHITECTURE.md](ARCHITECTURE.md) / [MODULE_MAP.md](MODULE_MAP.md)).
> Keep it current — see [README.md](README.md).

<!-- Suggested shape (adapt freely; delete parts that don't apply):

     Part I — Concepts / theory: the domain ideas a user must understand before the features make
              sense. Define every term of art once, here, and use it consistently everywhere.
     Part II — The features/methods: one section per major capability — what it does, when to use
              it, how to interpret the output, its limits.
     Part III — The interface: every screen/panel/command, walked through in the order a user
              meets them.
     Part IV — Reference tables: lookups, defaults, keyboard shortcuts, config options.
-->

{{USER_MANUAL_BODY}}
